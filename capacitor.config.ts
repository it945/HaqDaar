import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'pk.haqdaar.app',
  appName: 'HaqDaar',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
